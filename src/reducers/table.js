const initialState = {
  data: ["example", "example", "example"],
};

const table = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default table;
